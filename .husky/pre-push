# Последний рубеж перед тем, как сломанный код уедет в удалённый репозиторий.
# pre-commit намеренно лёгкий (только eslint --fix на staged), поэтому типы
# и тесты проверяются здесь. Полную матрицу гоняет CI.
npm run type-check
npm test
